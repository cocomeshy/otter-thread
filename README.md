# thread

Cross-platform threading, synchronization, and mutex primitives

## Exports

```spawn, join, sleep, current_id, mutex_create, mutex_lock, mutex_unlock, mutex_destroy```n
## Install

```otter pkg add thread 1.0.0
otter pkg pull```n
